releaseName: tools
source:
  targetRevision: 0.0.0-BRANCH_SHORT_SHA  # update with output of publish:tools-chart CI job

image:
  tag: fix-frontend-version-static-env

load:
  enabled: true
  dumpKind: full
  allowHost: db-cluster-rw.zenith-staging.svc
  dbSecret: db-cluster-app
  b2ReadCredsSecret: backup-credentials
