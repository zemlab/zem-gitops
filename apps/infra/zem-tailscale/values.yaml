oauth:
  clientId: tailscale-CHANGEME-client-id
  clientSecretKey: tailscale-CHANGEME-client-secret

dnsConfig:
  enabled: false

subnetRouter: {}
  # hostname: subnet-router
  # routes:
  #   - "10.44.0.0/16"
