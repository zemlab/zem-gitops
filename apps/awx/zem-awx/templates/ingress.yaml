apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: awx
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: awx-service
      port:
        number: 80
  tls:
    - hosts:
        - {{ .Values.awx.hostname | quote }}
