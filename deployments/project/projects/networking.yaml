name: networking

services:
  omada:
    enabled: true
    releaseName: "omada"
    source:
      repoURL: https://gitlab.com/zemlab/zem-gitops
      targetRevision: main
      path: apps/networking/omada
