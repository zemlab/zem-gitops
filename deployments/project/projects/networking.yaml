name: networking

services:
  omada:
    enabled: true
    releaseName: "omada"
    source:
      repoURL: https://github.com/zemlab/zem-gitops
      targetRevision: main
      path: apps/networking/omada
