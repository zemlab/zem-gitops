name: networking

services:
  omada:
    enabled: true
    releaseName: "omada"
    source:
      repoURL: git@gitlab.com:zemlab/zem-gitops.git
      targetRevision: main
      path: apps/networking/omada
